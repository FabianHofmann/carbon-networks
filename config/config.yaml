configs:
  test: 0
  presentation: 0

report:
  path: "report/submissions/ecemp/presentation/presentation"

remote:
  # ssh: zecm
  # path: /home/users/h/hofmann/scratch/projects/co2-network
  ssh: z1
  path: /home/htc/mhofmann/SCRATCH/projects/co2-network

run:
  name: &runs
    - baseline
    - co2-only
    - h2-only
    - full
    - net-negative-0.1/baseline
    - net-negative-0.1/co2-only
    - net-negative-0.1/h2-only
    - net-negative-0.1/full
    - half-price/co2-only
    - half-price/full

    # old setup
    # - w-fc/baseline
    # - w-fc/co2-only
    # - w-fc/h2-only
    # - w-fc/full
    # - w-fc/net-negative-0.1/baseline
    # - w-fc/net-negative-0.1/co2-only
    # - w-fc/net-negative-0.1/h2-only
    # - w-fc/net-negative-0.1/full
    # - w-fc/half-price/co2-only
    # - w-fc/half-price/full

  scenarios:
    enable: true
  shared_resources: true
  disable_progressbar: true

comparison:
  default:
    - baseline
    - co2-only
    - h2-only
    - full
  net-negative-0.1:
    - net-negative-0.1/baseline
    - net-negative-0.1/co2-only
    - net-negative-0.1/h2-only
    - net-negative-0.1/full
  emission-reduction:
    - baseline
    - net-negative-0.1/baseline
    - co2-only
    - net-negative-0.1/co2-only
    - h2-only
    - net-negative-0.1/h2-only
    - full
    - net-negative-0.1/full
  emission-reduction-full:
    - full
    - net-negative-0.1/full
  subsidy:
    - baseline
    - half-price/co2-only
    - h2-only
    - half-price/full
  all: *runs
difference:
  default:
    - co2-only
    - h2-only
  h2-only-full:
    - h2-only
    - full
  net-negative-0.1:
    - net-negative-0.1/co2-only
    - net-negative-0.1/h2-only
  net-negative-0.1-h2-only-full:
    - net-negative-0.1/h2-only
    - net-negative-0.1/full
  emission-reduction-0.1-co2-only:
    - co2-only
    - net-negative-0.1/co2-only
  emission-reduction-0.1-h2-only:
    - h2-only
    - net-negative-0.1/h2-only
  emission-reduction-0.1-full:
    - full
    - net-negative-0.1/full
  baseline-co2-only:
    - co2-only
    - baseline
  baseline-h2-only:
    - h2-only
    - baseline
  baseline-full:
    - full
    - baseline
difference-comparison:
  emission-reduction-0.1:
    - emission-reduction-0.1-h2-only
    - emission-reduction-0.1-full
  baseline:
    - baseline-co2-only
    - baseline-h2-only
    - baseline-full

labels:
  baseline: "Baseline"
  co2-only: "CO$_2$-Grid"
  h2-only: "H$_2$-Grid"
  full: "Hybrid"

  half-price/co2-only: "Cheap CO$_2$ Grid"
  half-price/full: "H$_2$ & Cheap CO$_2$ Grid"

  net-neutral: "NZ"
  net-negative-0.1: "NN"

  net-negative-0.1/baseline: "Baseline (NN)"
  net-negative-0.1/co2-only: "CO$_2$-Grid (NN)"
  net-negative-0.1/h2-only: "H$_2$-Grid (NN)"
  net-negative-0.1/full: "Hybrid (NN)"

  w-fc/baseline: "Baseline"
  w-fc/co2-only: "CO$_2$-Grid"
  w-fc/h2-only: "H$_2$-Grid"
  w-fc/full: "Hybrid"
  w-fc/half-price/co2-only: "Cheap CO$_2$ Grid"
  w-fc/half-price/full: "H$_2$ & Cheap CO$_2$ Grid"
  w-fc/net-neutral: "NZ"
  w-fc/net-negative-0.1: "NN"
  w-fc/net-negative-0.1/baseline: "Baseline (NN)"
  w-fc/net-negative-0.1/co2-only: "CO$_2$-Grid (NN)"
  w-fc/net-negative-0.1/h2-only: "H$_2$-Grid (NN)"
  w-fc/net-negative-0.1/full: "Hybrid (NN)"

  carbon: "Captured Carbon"
  hydrogen: "Hydrogen"
  electricity: "Electricity"
  gas: "Natural Gas"
  co2: "Atm. CO$_2$"
